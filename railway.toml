[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
healthCheckPath = "/health"
healthCheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "pais"
port = 8080

[env]
DATABASE_URL = "sqlite:///./pais.db"
ENVIRONMENT = "production"
